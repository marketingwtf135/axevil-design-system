// @axevil/design-system entry — re-exports the Tailwind token object.
// Consumed as `const { tokens } = require('@axevil/design-system')` in tailwind.config.
const { tokens } = require('./tailwind.config.base')
module.exports = { tokens }
